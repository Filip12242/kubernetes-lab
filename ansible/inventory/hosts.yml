all:
  children:
    control_plane:
      hosts:
        k8s-cp1:
          ansible_host: 192.168.100.11
    workers:
      hosts:
        k8s-w1:
          ansible_host: 192.168.100.12
        k8s-w2:
          ansible_host: 192.168.100.13
    # Convenience group covering every node in the cluster.
    k8s_cluster:
      children:
        control_plane:
        workers:
